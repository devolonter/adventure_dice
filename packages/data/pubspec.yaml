name: data
description: A data layer for the application.
publish_to: 'none'

environment:
    sdk: ">=3.0.0 <4.0.0"

dependencies:
    domain:
        path: ../domain
    isar: ^3.1.0+1
dev_dependencies:
    build_runner: ^2.4.4
    isar_generator: ^3.1.0+1
